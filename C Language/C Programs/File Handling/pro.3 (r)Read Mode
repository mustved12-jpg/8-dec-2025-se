#include<stdio.h>
int main()
{
	FILE *fp;
	char c;
	fp=fopen("my file.txt","r");
	if(fp==NULL)
	{
		printf("fot found");
		return 0;
	}
	while((c=fgetc(fp))!=EOF)
	{
		printf("%c",c);
	}
	fclose(fp);
}
/* output:hello this is my first file programe
*/
