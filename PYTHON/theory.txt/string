string:
~> string is a collection of charectors
~> which is represent by double quotes or single quotes tripple quotes

    e.g.
        subject="python"
            OR
        subject='python'
            OR
        subject="""python"""

~> string index always start from 0

    e.g
    name="python"
    print(name[0])

    outpu:p
