//Appliction that perform all string related operation:
#include<stdio.h>
#include<string.h>
#include<stdbool.h>
int rev()
{
	char name[20];
	printf("\nenter a string:");
	gets(name);
	strrev(name);
	printf("\n\t\t------>after reversing:-%s",name);
	
}
int con()
{
	char name1[20],name2[20];
	printf("\nenter a first string :");
	gets(name1);
	printf("\nenter a secound string :");
	gets(name2);
	strcat(name1,name2);
	printf("\n\t\t------>after concatenation :%s",name1);
}
int cop()
{
	char name1[20],name2[20];
	char co;
	printf("\nenter a string :");
	gets(name1);
	strcpy(name2,name1);
	printf("\n\t\t------>copy :%s",name2);
}
int lan()
{
	char name[20];
	printf("\nenter a string:");
	gets(name);
	printf("\n\t\t------>lanth is=%d",strlen(name));
}
void pali()
{
	char name[20];
	int s,p=1;
	printf("enter a string :");
	gets(name);
	s=strlen(name);
	for(int i=0;i<s;i++,s--)
	{
		if(name[i]!=name[s-1])
		{
			p=0;break;
		}
	}
	if(p==0)
	{
		printf("\n\t\t------>%s is not a palindrom!!!",name);
	}
	else
	{
		printf("\n\t\t------>%s is a <<<palindrome>>>",name);
	}
}
void fre()
{
	char name[20],ch;
	printf("enter a string :");
	gets(name);
	printf("enter a carecter :");
	scanf(" %c",&ch);
	int max=0,s=strlen(name);
	for(int i=0;i<s;i++)
	{
		if(name[i]==ch)
		{
			max++;
		}
	}
	printf("\n\t\t------>Frequency of (%c) is =%d",ch,max);
}
void fin()
{
	char name[20];
	char ovel[6]="aeiou";
	int o=0,c=0,s=0;
	printf("enter a string :");
	gets(name);
	s=strlen(name);
		for(int i=0;i<s;i++)
		{
			for(int j=0;j<5;j++)
			{
				if(name[i]==ovel[j])
				{
					o++;break;	
				}
			}
		}
		for(int i=0;i<s;i++)
		{
			if(name[i]>=97&&name[i]<=122)
			{
				c++;
			}
		}
			c-=o;
		printf("\n\t\t------>vowle =%d",o);
		printf("\n\t\t------>consonants =%d",c);
} 
void blank()
{
	char name[20];
	printf("enter a string :");
	gets(name);
	int space=0,count=0,s=strlen(name);
	for(int i=0;i<s;i++)
	{
		if(name[i]==' ')
		{
			space++;
		}
		else if(name[i]>48&&name[i]<57)
		{
			count++;
		}
	}
	printf("\n\t\t------>number is :%d\n\t\t------>blank spaces is :%d",count,space);
}
main()
{
	bool t=true;
	int ch,co;
	while(t)
	{
		printf("\n\n:::MAIN MENU:::");
		printf("\n\t\t1.reverse a string\n\t\t2.concatination\n\t\t3.palindrome");
		printf("\n\t\t4.copy\n\t\t5.length of the string\n\t\t6.frequency of character");
		printf("\n\t\t7.number of vowel and consonants\n\t\t8.blank spaces and digits\n\t\t9.§§§EXIT§§§");
		printf("\nenter a choise:");
		scanf("%d",&ch);
		getchar();
		if(ch==9)
		{
			t=false;
		}
		else if (ch>0&&ch<=8)
		{
			t=true;
		}
		else
		{
				printf("\n\n\t\t!!! WRONG CHOICE!!!  TRY AGAIN...");
				continue;
		}
		switch(ch)
		{
			case 1:rev();break;
			        
			case 2:con();break;
			
			case 3:pali();break;
			
			case 4:cop();break;
			
			case 5:lan();break;
			
			case 6:fre();break;
			
			case 7:fin();break;
			
			case 8:blank();break;
			
		}
		printf("\n\n\t\t:::DO YOU WANT TO CONTINUE:::§§PRESS->> (1) :- ");
		scanf("%d",&co);
		if(co==1)
		{
			continue;
		}
		else 
		{
			t=false;
		}
	}
}